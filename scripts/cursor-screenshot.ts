import { chromium, devices } from 'playwright';\n\nasync function main() {\n  const browser = await chromium.launch({ headless: true });\n  const context = await browser.newContext({\n    viewport: { width: 1440, height: 900 },\n    colorScheme: 'dark',\n    userAgent: 'Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36',\n    deviceScaleFactor: 1,\n  });\n  const page = await context.newPage();\n  await page.goto('https://cursor.com', { waitUntil: 'domcontentloaded', timeout: 60000 });\n  // Give it a moment for animations/content\n  await page.waitForTimeout(3000);\n  await page.screenshot({ path: '/workspace/.playwright-mcp/cursor-dark-1440x900.png', fullPage: true });\n  await browser.close();\n}\n\nmain().catch((err) => {\n  console.error(err);\n  process.exit(1);\n});\n